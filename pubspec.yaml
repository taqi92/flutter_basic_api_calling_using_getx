name: flutter_api_calling_using_getx
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter



  cupertino_icons: ^1.0.0


  connectivity: ^2.0.2 # for checking internet connectivity
  get: ^3.24.0 # getx package
  http:
  google_fonts: ^1.1.0
  flutter_secure_storage: ^3.3.1+1



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/logos/
    - assets/icons/
    - assets/images/
